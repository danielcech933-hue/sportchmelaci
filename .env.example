# GIPHY fallback provider for the chat GIF picker.
# Keep this key in the Lovable/Vite environment, never commit a real key.
VITE_GIPHY_API_KEY=

# KLIPY primary provider for the chat GIF/sticker picker.
# Prefer the Lovable KLIPY connector / server-side secret in production.
# This variable is only the local development fallback for the direct KLIPY adapter.
VITE_KLIPY_API_KEY=
